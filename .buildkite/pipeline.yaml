agents:
  queue: kubernetes
steps:
- label: ":git:"
  plugins:
  - kubernetes:
      podSpec:
        containers:
        - image: alpine:latest
          command: [ash]
          args:
          - -c
          - '"sleep 60 && echo hello world"'
